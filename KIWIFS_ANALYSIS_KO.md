# 🥝 KiwiFS 분석 및 활용 정리 (한국어)

> 이 문서는 KiwiFS 레포지토리를 직접 분석하고 정리한 한국어 가이드입니다.
> 설치법, 정체(플러그인/MCP 구분), 인증, 인기 요인, 로컬 에이전트 활용, 수익화 아이디어,
> React/PHP 재구현 가능성까지 다룹니다.

## 🔗 관련 링크

| 구분 | 주소 |
|---|---|
| **내 레포지토리** | https://github.com/bmshin94/kiwifs |
| **원본(업스트림) 레포** | https://github.com/kiwifs/kiwifs |
| 공식 문서 | https://docs.kiwifs.com |
| Storybook 데모 | https://demo.kiwifs.com/storybook/ |
| 클라우드(유료 호스팅) | https://app.kiwifs.com |
| Docker Hub | https://hub.docker.com/r/ameliaanhlam/kiwifs |
| 상용 라이선스 문의 | amelia.anh.lam@gmail.com |

---

## 목차

1. [KiwiFS가 뭐야?](#1-kiwifs가-뭐야)
2. [폴더 구조](#2-폴더-구조)
3. [설치 및 사용법](#3-설치-및-사용법)
4. [플러그인? 스킬? MCP?](#4-플러그인-스킬-mcp)
5. [API 토큰이 필요한가?](#5-api-토큰이-필요한가)
6. [왜 깃허브에서 인기가 있을까](#6-왜-깃허브에서-인기가-있을까)
7. [로컬 에이전트 구축에 도움이 될까](#7-로컬-에이전트-구축에-도움이-될까)
8. [수익화 아이디어](#8-수익화-아이디어)
9. [React나 PHP로 만들 수 있을까](#9-react나-php로-만들-수-있을까)
10. [최종 요약](#10-최종-요약)

---

## 1. KiwiFS가 뭐야?

**한 줄 요약: 마크다운 폴더를 "나만의 노션"으로 바꿔주는 단일 Go 바이너리 서버.**

```
📁 내메모/*.md  ──[ kiwifs serve ]──►  🌐 localhost:3333
     (원본 그대로 남음)                   검색 + 위키링크 + 그래프 + 다크모드
```

핵심 철학은 **"파일이 곧 진실(source of truth)"**입니다.
검색 인덱스나 벡터 DB는 전부 **언제든 다시 만들 수 있는 파생물**이고,
프로그램을 지워도 `.md` 파일은 그대로 남습니다. → **벤더 락인 제로**

### 레포 규모 (직접 측정)

| 항목 | 수치 |
|---|---|
| Go 소스 파일 | 497개 |
| Go 코드 라인 | 약 136,000줄 |
| TypeScript/TSX 파일 | 394개 |
| `internal/` 패키지 | 54개 |
| MCP 도구 | 70개 |
| CLI 명령어 | 25개 이상 |
| 데이터 임포터 | 19종 |
| 현재 버전 | v0.19.62 |

### 핵심 기능

- **검색**: SQLite FTS5(BM25) 전문검색 + 벡터(의미) 검색
- **버전관리**: 쓰기 1회 = git 커밋 1개 (자동 감사 추적)
- **접속 프로토콜 6종**: REST / MCP / NFS / S3 / WebDAV / FUSE
- **DQL**: frontmatter를 SQL처럼 쿼리 (`TABLE`, `WHERE`, `SORT`, `GROUP BY`)
- **웹 UI**: React 19 + Tailwind 4 + shadcn/ui, `go:embed`로 바이너리에 내장
- **에이전트 메모리**: `episodes/`(일화) + `pages/`(정리된 지식), 만료·모순 탐지
- **기타**: 워크플로 엔진, JSON Schema 검증, 웹훅, 퍼블리싱, 문서 출력(PDF/슬라이드), 캔버스, 화이트보드

---

## 2. 폴더 구조

```
kiwifs/
├── main.go              # 진입점
├── cmd/                 # CLI 명령어 (serve, init, mcp, query, import, export, token, ...)
├── internal/            # 백엔드 핵심 (54개 패키지)
│   ├── api/             # REST API (Echo 프레임워크)
│   ├── pipeline/        # 쓰기 파이프라인 (저장 → git → 인덱싱 → SSE)
│   ├── search/          # grep + SQLite FTS5 + 메타데이터 인덱스
│   ├── vectorstore/     # 벡터 백엔드 (OpenAI/Ollama/ONNX/Cohere/Qdrant/pgvector/Pinecone)
│   ├── versioning/      # git, copy-on-write
│   ├── mcpserver/       # MCP 서버 (도구 70개)
│   ├── dataview/        # DQL 파서 (직접 만든 Pratt 파서)
│   ├── memory/          # 에이전트 장기기억
│   ├── importer/        # 19종 임포터 (Confluence 관련만 7개 파일)
│   ├── workflow/        # 상태머신 워크플로
│   ├── nfs/ s3/ webdav/ fuse/   # 프로토콜 서버들
│   └── ...
├── pkg/kiwi/            # 공개 Go 라이브러리 (다른 Go 앱에 임베드)
├── ui/                  # React 프론트엔드 (npm 패키지명 @kiwifs/ui, 라이선스 MIT)
├── docs/                # API / 아키텍처 / FAQ / 로드맵 / 예제
├── wiki/                # 유즈케이스 11종 (UC-1 ~ UC-11)
├── episodes/agents/     # 실제 AI 에이전트 작업 기록
└── .github/workflows/   # CI 9종 (테스트, 릴리즈, 스팸필터, 위키동기화 등)
```

---

## 3. 설치 및 사용법

### 설치 (4가지 중 택 1)

```bash
# ① 가장 간단 (macOS / Linux)
curl -fsSL https://raw.githubusercontent.com/kiwifs/kiwifs/main/install.sh | sh

# ② Homebrew
brew install kiwifs/tap/kiwifs

# ③ Go 설치돼 있으면
go install github.com/kiwifs/kiwifs@latest

# ④ Docker (설치 자체가 싫을 때)
docker run -p 3333:3333 -v ./knowledge:/data ameliaanhlam/kiwifs
```

> `install.sh`는 OS/CPU를 감지해서 깃허브 릴리즈 압축파일을 받아 `/usr/local/bin`에 넣는 게 전부입니다.
> Windows는 ①②를 지원하지 않으므로 Docker나 직접 빌드를 사용하세요.

### 사용 (3단계)

```bash
kiwifs init --template knowledge --root ./knowledge   # 1) 지식 폴더 생성
kiwifs serve --root ./knowledge                        # 2) 서버 실행
# 3) 브라우저에서 http://localhost:3333 접속
```

### 소스에서 직접 빌드 (이 레포 기준)

```bash
make build      # UI(React) 빌드 후 Go 바이너리에 내장
make go-build   # UI 변경 없을 때 Go만 빌드
make dev        # 개발 서버 실행 (:3333)
make dev-ui     # Vite 핫리로드 (UI 작업용)
go test ./... -race   # 테스트
go vet ./...          # 린트
```

⚠️ `make build`는 **Go 1.25 이상 + Node/npm**이 모두 필요합니다.

### 에이전트에서 쓰기 (REST 예시)

```bash
curl -X PUT 'localhost:3333/api/kiwi/file?path=pages/auth.md' \
  -H "X-Actor: my-agent" \
  -d "# 인증\n\nOAuth2 + JWT..."
```

`X-Actor` 헤더가 git 커밋 작성자로 기록됩니다. (누락 시 REST는 `anonymous`)

---

## 4. 플러그인? 스킬? MCP?

**정답: 셋 다 아니고 "독립 서버"입니다.**

| 오해 | 실제 |
|---|---|
| ❌ 플러그인 | Claude Code에 꽂는 확장이 아님 |
| ❌ 스킬 | 프롬프트 문서가 아님 |
| 🔺 MCP | **기능 중 하나**일 뿐 (접속 통로 6개 중 1개) |

```
        [ KiwiFS 서버 ]
   ┌────┬────┬────┬────┬────┬────┐
  REST  MCP  NFS  S3  WebDAV FUSE   ← 접속 방법 6가지
```

### MCP로 AI 도구에 연결하기

```bash
kiwifs mcp --root ~/knowledge
```

또는 Claude Desktop / Cursor 설정 파일에:

```json
{
  "mcpServers": {
    "kiwifs": {
      "command": "kiwifs",
      "args": ["mcp", "--root", "/path/to/knowledge"]
    }
  }
}
```

자동 설정도 지원합니다 (cursor / claude-code / claude-desktop / windsurf 감지):

```bash
kiwifs connect my-workspace --write auto
```

### 제공되는 MCP 도구 (70개, 일부)

`kiwi_search` `kiwi_write` `kiwi_read` `kiwi_remember` `kiwi_forget`
`kiwi_query` `kiwi_search_semantic` `kiwi_search_hybrid` `kiwi_backlinks`
`kiwi_graph_path` `kiwi_graph_centrality` `kiwi_task_create` `kiwi_workflow_advance`
`kiwi_memory_report` `kiwi_health_check` `kiwi_export_document` ...

---

## 5. API 토큰이 필요한가?

### 로컬에서 혼자 쓰면 → **불필요** ✅

`internal/api/server.go`의 인증 미들웨어를 확인한 결과,
**키 설정이 비어 있으면 그대로 통과**시킵니다. 즉 기본값은 "인증 꺼짐"입니다.
`kiwifs mcp` 모드는 아예 서버를 거치지 않고 파일을 직접 읽고 쓰므로 토큰 개념 자체가 없습니다.

### 토큰이 필요한 경우

| 상황 | 필요한 것 |
|---|---|
| 인터넷에 서버를 공개 | 토큰 필수 ⚠️ |
| 팀원과 공유 | 토큰 또는 OIDC |
| KiwiFS Cloud 사용 | 해당 서비스의 API 키 (유료) |

### 토큰 관리 명령어

```bash
kiwifs token create --scope read --actor ci-bot   # scope: read | write | admin
kiwifs token create --scope read --prefix docs/   # 경로 제한도 가능
kiwifs token list
kiwifs token revoke kiwi_ro_abc1
```

> 💡 `api.kiwifs.com`(클라우드)은 **별도 유료 서비스**입니다.
> 내 PC에 설치해서 쓰는 것은 **무료이며 토큰도 불필요**합니다.

---

## 6. 왜 깃허브에서 인기가 있을까

> ⚠️ 실제 스타 개수는 확인하지 않았습니다. 아래는 레포 내용을 근거로 한 "인기 요인 분석"입니다.

### ① 타이밍
지금 개발자들의 최대 관심사가 **"AI 에이전트에게 어떻게 기억을 줄 것인가"**입니다.
이 프로젝트는 `episodes/`(일화 기억), `pages/`(정리된 지식), 만료, 모순 탐지까지
**전용 설계**로 갖추고 있어 유행의 정중앙에 있습니다.

### ② "단일 바이너리" 감성
PocketBase가 인기를 끈 이유와 동일합니다.
**DB 설치 불필요, 프론트 배포 불필요, 설정 불필요, CGo 없음.**

### ③ 벤더 락인 제로
데이터가 그냥 `.md` 파일이라 `git clone` 한 번이면 전부 가져갈 수 있습니다.
**심리적 진입장벽이 0**입니다.

### ④ 관리 상태가 매우 좋음
- v0.19.62까지 릴리즈, release-please 자동화
- CI 9종, 이슈 템플릿, 스팸 필터, 위키 자동 동기화
- 컨트리뷰터 9명, Storybook 데모 사이트 운영
- FAQ / 아키텍처 / API / 로드맵 / 예제 / POSIX 호환성 문서 완비

### ⑤ 홍보하기 좋은 숫자
"MCP 도구 70개, 프로토콜 6개, 임포터 19종" — 트위터/HN에서 먹히는 스펙입니다.

---

## 7. 로컬 에이전트 구축에 도움이 될까

### A. "가져다 쓰기" 관점 → ⭐⭐⭐⭐⭐

로컬 에이전트를 만들 때 귀찮은 문제들을 통째로 해결해 줍니다.

| 문제 | KiwiFS의 답 |
|---|---|
| 기억을 어디에 저장? | 마크다운 파일 |
| 저장한 걸 어떻게 검색? | FTS5 + 벡터 검색 내장 |
| 사람은 어떻게 확인? | 웹 UI 내장 |
| 잘못 쓴 건 어떻게 되돌려? | git 자동 커밋 |

개발자는 **"에이전트 두뇌 로직"에만 집중**하면 됩니다.

### 완전 오프라인 구성도 가능

```bash
kiwifs model download all-minilm-l6-v2   # 로컬 ONNX 임베딩 모델
go build -tags onnx -o kiwifs .
```

→ **OpenAI 키 없이도 의미 검색** 가능. 인터넷 없이 동작합니다.
한국어 컬렉션은 `multilingual-e5-small` + `query_prefix`/`passage_prefix` 설정을 권장합니다.

### B. "설계 배우기" 관점 → ⭐⭐⭐⭐⭐

`wiki/UC-5-Agent-Memory.md`의 핵심 문장:

> **"에이전트는 펜이고, LLM은 두뇌이며, KiwiFS는 종이다."**
> (저장소가 직접 LLM을 돌려 판단하지 않는다 — 그건 에이전트의 역할)

에이전트 설계에서 가장 어려운 **"어디까지가 저장소의 일이고 어디부터가 AI의 일인가"**에 대한
답이 실제 동작하는 코드로 들어 있습니다.

### ⚠️ 주의
KiwiFS는 **에이전트 프레임워크가 아닙니다.**
LangChain/CrewAI를 대체하는 게 아니라, 그 옆에 붙는 **"기억 창고"** 역할입니다.

---

## 8. 수익화 아이디어

### 8-0. 라이선스 경계선 (BSL 1.1 원문 기준)

#### ✅ 명시적으로 허용된 것

> **(a)** 이것으로 **자신의 앱이나 사내 서비스**를 만들어 운영
> **(b)** 더 큰 애플리케이션의 **부품으로 포함** (단, 앱의 핵심 가치가 KiwiFS 자체면 안 됨)
> **(c)** **컨설팅 / 기술지원 / 통합 서비스** 판매

#### ❌ 금지된 것 (하나뿐)

> **Competing Service** — KiwiFS의 기능을 제3자에게 **매니지드/호스팅 형태로 제공**하는 것
> (예: "KiwiFS 서버 대신 돌려드립니다, 월 3만원")

#### ⚠️ 상표권

"KiwiFS", 로고, 트레이드 드레스는 등록 상표입니다.
- 파생 제품 이름에 "KiwiFS", "Kiwi FS" 등 유사 명칭 사용 ❌
- 로고/트레이드 드레스 사용 ❌
- 공식 제휴인 것처럼 표현 ❌
- **"KiwiFS 기반", "KiwiFS 호환" 같은 사실 설명은 허용(fair use 명시)** ✅

#### 🕐 4년 뒤 자동 해제
각 릴리즈는 **4년 후 Apache 2.0**으로 전환됩니다. (2026년 버전 → 2030년 완전 자유)

---

### 8-1. 팔 수 있는 "무기" 인벤토리

| 기능 | 상품화 포인트 |
|---|---|
| 임포터 19종 (Confluence/Notion/Obsidian/Postgres/MySQL/Mongo/ES/Excel/GSheets...) | **마이그레이션 서비스의 핵심** |
| 감사 로그 + git 커밋 + SHA-1 해시 체인 | 금융/의료/법무 **규제 대응** |
| 문서 출력 (Typst/XeLaTeX PDF, HTML, Marp 슬라이드, MkDocs) | 보고서 자동화 |
| 워크플로 엔진 (상태머신 + 칸반) | 결재/승인 프로세스 |
| JSON Schema 검증 | 문서 품질관리 |
| 웹훅 (HMAC 서명, 재시도) | 슬랙/CI 알림 자동화 |
| 퍼블리싱 (`/p/*`, 공유링크 토큰) | 고객사 문서 포털 |
| 분석 (오래된 문서/끊긴 링크/고아 문서/검색 실패) | **문서 헬스체크 리포트** |
| 템플릿 12종 (`kb` `wiki` `memory` `tasks` `data` `cms` `runbook` `adr` `prompt` `research` `log` `blank`) | 직군별 팩 판매 |
| `@kiwifs/ui` (React 컴포넌트, **MIT 라이선스**) | UI 자유 활용 |

---

### 8-2. 아이디어 7선

> 💸 아래 가격은 모두 **추정치**입니다. 실제 시장 반응에 따라 조정 필요.

#### 🥇 ① 사내 위키 이전(마이그레이션) 서비스
- **한 줄**: "컨플루언스 라이선스비 아까우시죠? 그대로 옮겨드립니다"
- **근거**: Confluence/Notion 팀 요금 인당 월 1~2만원 → 50명이면 연 600~1,200만원
- **강점**: `internal/importer/confluence*.go`가 7개 파일. XHTML 변환·매크로·링크 재작성·첨부파일 처리 완비
- **가격**: 구축 300만~1,500만원 / 연 유지보수 구축비의 15~20%
- **난이도**: ⭐⭐ (개발보다 영업이 어려움)
- **라이선스 안전도**: ✅✅✅ (c)항에 명시적 허용

#### 🥈 ② 한국어 검색 특화 패키지 ⭐ 가장 추천
- **한 줄**: "원본은 한국어 검색이 약합니다. 튜닝해 드립니다"
- **근거**: 한국어 관련 언급이 README에 **단 한 줄**뿐. SQLite FTS5는 기본적으로 공백 분리라 조사/어미 처리가 안 됨
- **가격**: 튜닝 200만~800만원 / 형태소 플러그인 연 100만~300만원 / 임베딩 파인튜닝 500만원~
- **보너스**: 업스트림에 PR → 컨트리뷰터 등재 → 영업 신뢰도 급상승

#### 🥉 ③ AI 에이전트 기억 저장소 (자체 제품화)
- **구조**: `[내 브랜드 제품] = 자체 UI + 에이전트 로직(핵심) + KiwiFS(내부 부품)`
- **라이선스 포인트**: (b)항 조건대로 **"핵심 가치는 에이전트 로직"**이어야 함
- **가격**: SaaS 팀당 월 5만~30만원 / 온프레미스 연 1,000만원~
- **난이도**: ⭐⭐⭐⭐

#### ④ 규제 산업 특화 버전 (단가 최고)
- **타겟**: 병원 / 은행 / 보험 / 법무법인 / 제약 / 방산

| 규제 요구 | 대응 기능 |
|---|---|
| 누가 언제 무엇을 변경했나 | git 커밋 + `git blame` (줄 단위) |
| 변조 불가 기록 | SHA-1 해시 체인 |
| 감사 로그 | `[audit]` 설정 + 조회 API |
| 권한 분리 | 토큰 scope + 경로 제한 |
| 데이터 국외반출 금지 | 100% 자체 호스팅 |
| 백업/복구 | `kiwifs backup` / `restore` |

- **가격**: 구축 1,000만~5,000만원 / 연 유지보수 500만원~
- **⚠️ 주의**: 로드맵상 **RBAC이 아직 미구현(`[ ]`)** → 직접 구현 필요 (= 차별화 기회)

#### ⑤ AI 문서 헬스케어 SaaS
- **한 줄**: "당신 회사 문서, 30%가 썩었습니다"
- **구현**: `kiwifs analytics` 결과 + Typst PDF 출력 + 스케줄러 조합
- **가격**: 월 10만~50만원 / 1회 진단 150만원
- **난이도**: ⭐⭐ **가장 빨리 MVP 가능**

#### ⑥ 템플릿·플러그인 마켓
- 스타트업 위키 팩(5~20만원), 장애 대응 런북 팩(10만원), 법무 계약관리 팩(30만원),
  연구실 논문관리 팩(10만원), 1인 개발자 팩(3만원)
- **장점**: 재고/배송 없음, 무한 복제, 컨설팅 영업의 미끼상품
- **난이도**: ⭐ **가장 쉬움 — 첫 시작으로 추천**

#### ⑦ 콘텐츠 + 교육 (초기 자금 마련)
- 강의 5~15만원 / 뉴스레터 월 1만원 / 기업 워크샵 100만~300만원 / 전자책 2~5만원
- **효과**: 자본금 0원 시작 → 전문성 확보 → **수강생이 컨설팅 고객으로 전환**

---

### 8-3. 로드맵 빈칸 = 숨은 기회

`docs/ROADMAP.md`에서 아직 **미완성(`[ ]`)**인 항목들:

| 미완성 항목 | 사업 기회 |
|---|---|
| **RBAC 권한관리 (Casbin)** | 🔥 기업 도입의 최대 걸림돌. 구현 시 엔터프라이즈 진입 |
| JS 훅 (`.kiwi/hooks/*.js`) | 고객사별 커스텀 로직 = 유료 개발 건 |
| Go 파이프라인 훅 (`OnBeforeWrite`/`OnAfterWrite`) | 동일 |
| React 컴포넌트 라이브러리 npm 배포 | UI 사업 확장 |
| Docusaurus 익스포트 | 문서 사이트 자동 생성 수요 |
| 콘텐츠 생명주기 (보존정책, 법적 보존) | 규제 산업 필수, 단가 높음 |

**전략**: 이 중 하나를 오픈소스로 기여 → 컨트리뷰터 등재 → 영업 신뢰도 확보

---

### 8-4. 한국 시장 특수성

| 요소 | 기회 🟢 / 위협 🔴 |
|---|---|
| 망분리 규제 (금융/공공) | 🟢 클라우드 사용 불가 → 자체 호스팅이 유일한 답 |
| 개인정보보호법 | 🟢 국외 서버 기피 → 국내 구축 선호 |
| 한국어 검색 품질 | 🟢 원본이 약함 → 차별화 포인트 |
| 네이버웍스/카카오워크 점유 | 🔴 문서 영역만 공략 필요 |
| "오픈소스 = 공짜" 인식 | 🔴 유지보수비 설득 필요 |
| 레퍼런스 중시 문화 | 🔴 첫 고객이 난관 → 무료 1호 고객 전략 필수 |

---

### 8-5. 90일 실행 플랜

**1개월차 — 손에 익히기 (투자 0원)**
- [ ] 직접 설치하고 매일 사용해 보기
- [ ] 한국어 검색 품질 직접 테스트 → 문제점 목록화
- [ ] 템플릿 팩 1개 제작
- [ ] 블로그 글 3편 (설치기 / 한국어 검색 문제 / AI 기억 만들기)

**2개월차 — 증명하기**
- [ ] 한국어 튜닝 실제 성공
- [ ] 업스트림에 PR 1건 → 컨트리뷰터 등재
- [ ] 지인 회사 1곳 무료 구축 → 사례 확보
- [ ] 문서 헬스체크 리포트 MVP

**3개월차 — 매출 발생**
- [ ] 사례 기반 랜딩 페이지 제작
- [ ] 템플릿 팩 유료 판매 개시
- [ ] 컨설팅 유료 1호 고객 영업
- [ ] 워크샵 1회 진행

---

### 8-6. 리스크 체크리스트

| 리스크 | 대응 |
|---|---|
| 원작자가 라이선스를 더 조임 | 이미 배포된 버전에 소급 적용 안 됨. 버전 고정 |
| 원작자 클라우드와 충돌 | "호스팅 대행" 절대 금지. 컨설팅/구축/자체제품만 |
| 상표권 | 자체 브랜드 사용, "KiwiFS 기반" 표기만 |
| 아직 v0.19 (API 변경 가능) | 고객사 계약 시 버전 고정 + 업그레이드 유상 |
| 프로젝트 중단 위험 | 데이터가 순수 `.md` 파일 → 고객 데이터는 안전. **오히려 영업 포인트** |
| 혼자서 전부 감당 불가 | ⑥ → ⑤ → ② → ① 순서로 작은 것부터 |

---

### 8-7. 추천 실행 순서

```
1단계 (0원, 1개월)  →  ⑥ 템플릿 팩 판매        = 감 잡기 + 첫 매출
2단계 (2개월)       →  ⑤ 문서 헬스체크 리포트   = 기존 기능 조합, 빠른 제품화
3단계 (3개월)       →  ② 한국어 튜닝 + PR 기여  = 기술 차별화 + 신뢰도
4단계 (6개월~)      →  ① 기업 구축 컨설팅       = 본격 수익 (건당 수백~수천만원)
5단계 (1년~)        →  ③ 자체 브랜드 제품       = 스케일
```

**핵심 원칙 3가지**
1. **"KiwiFS를 파는 게 아니라, KiwiFS로 해결한 문제를 판다"**
2. **한국어 + 망분리 + 규제** — 이 세 가지가 해자(moat)
3. 작게 시작해서 사례부터 쌓기. 첫 고객이 가장 어렵다

---

## 9. React나 PHP로 만들 수 있을까

### 중요: React 버전은 이미 존재합니다

`ui/` 폴더가 통째로 React입니다.
- React 19 + TypeScript + Vite + Tailwind 4 + shadcn/ui
- 파일 394개, Storybook 데모 포함
- **npm 패키지 `@kiwifs/ui`로 빌드되며 라이선스가 MIT** (백엔드 BSL과 별개!)

즉 새로 만들 필요 없이 **부품으로 가져다 쓸 수 있습니다.**

### 난이도 비교

| 접근 | 난이도 | 평가 |
|---|---|---|
| React로 UI만 새로 제작 | ⭐⭐ | 백엔드는 KiwiFS, 화면만 직접 → **가장 추천** |
| PHP로 백엔드 미니 버전 | ⭐⭐⭐ | 기본 기능만이면 충분히 가능 |
| 전체 복제 | ⭐⭐⭐⭐⭐ | Go 13만 줄 + NFS/S3/FUSE 자체 구현 → 비현실적 |

### 추천 1: React 프론트 + KiwiFS 백엔드

```
[직접 만든 React 앱]  ──REST API──►  [kiwifs serve]
   디자인/UX 자유                  검색·git·AI 처리 위임
```

### 추천 2: PHP로 "미니 버전" (학습용으로 최적)

| 기능 | PHP 구현 가능성 |
|---|---|
| 마크다운 읽기/쓰기 | ✅ 파일 입출력 |
| 마크다운 렌더링 | ✅ `league/commonmark` |
| 전문 검색 | ✅ SQLite FTS5 (PHP 지원) |
| git 자동 커밋 | ✅ `exec('git commit')` |
| `[[위키링크]]` 파싱 | ✅ 정규식 |
| 벡터 검색 | 🔺 난이도 있음 |
| NFS / S3 / FUSE | ❌ 비권장 |

핵심 기능만 추리면 **주말 프로젝트로 충분**하며, 만드는 과정에서 배우는 게 많습니다.

### ❌ 비권장: 전체 복제
FUSE 드라이버, NFS 서버, S3 호환 API, DQL Pratt 파서는 각각이 별도 프로젝트 규모입니다.

---

## 10. 최종 요약

| 질문 | 한 줄 답 |
|---|---|
| 이게 뭐야? | 마크다운 폴더를 위키 서버로 바꿔주는 단일 Go 바이너리 |
| 설치 | `curl ... \| sh` → `kiwifs init` → `kiwifs serve` |
| 플러그인/스킬/MCP? | **독립 서버**. MCP는 접속 통로 6개 중 하나 |
| API 토큰 | 로컬 단독 사용은 **불필요**, 외부 공개 시에만 필요 |
| 인기 요인 | AI 기억 트렌드 + 단일 바이너리 + 락인 제로 + 뛰어난 관리 상태 |
| 로컬 에이전트 | ⭐⭐⭐⭐⭐ 기억 창고로 최적. 완전 오프라인도 가능 |
| 수익화 | "이것을 파는 것"❌ / "이것으로 만든 것을 파는 것"⭕ → **한국어 + 규제 시장이 답** |
| React/PHP | React UI는 **이미 존재(MIT)**, PHP 미니 버전은 학습용으로 추천 |

---

*이 문서는 https://github.com/bmshin94/kiwifs 레포지토리를 직접 분석하여 작성되었습니다.*
